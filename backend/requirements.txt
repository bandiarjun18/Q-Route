# Q-Route Backend – Python dependencies
# Pin only the major version so minor/patch updates are still pulled in;
# the exact pinned lock will be generated with `pip freeze > requirements-lock.txt`.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Scientific computing & data handling
numpy>=1.26.0
pandas>=2.2.0

# Graph modelling & shortest-path algorithms
networkx>=3.3

# Data validation (used by FastAPI internally, explicit pin for clarity)
pydantic>=2.7.0

# HTTP client (used in tests to call the API)
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
