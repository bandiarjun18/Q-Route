# Q-Route PostgreSQL Configuration Example
# Copy this file to .env and adjust credentials for your local environment
DATABASE_URL=postgresql+psycopg://postgres:your_password@localhost:5432/qroute

# Database connection pool configuration
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800
